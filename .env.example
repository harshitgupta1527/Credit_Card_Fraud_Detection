# --- Security Configurations ---
# Generate a secure random 32-byte hex string (e.g. openssl rand -hex 32)
SECRET_KEY=9f8a3d6b2c5e1f0a7b4c8d3e9f0a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a

# --- Database Configurations ---
# SQLite local db by default. In production change to postgresql://user:pwd@host/dbname
DATABASE_URL=sqlite:///./fraud_detection.db

# --- CORS Configurations ---
# Comma-separated list of allowed origin URLs (no trailing slashes)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173
